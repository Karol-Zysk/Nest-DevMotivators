export enum Place {
  main = 'main',
  waiting = 'waiting',
  staging = 'staging',
}

export enum Role {
  admin = 'admin',
  moderator = 'moderator',
  user = 'user',
}

export enum VoteKind {
  like = 'like',
  dislike = 'dislike',
}
export enum VoteMethod {
  give = 'push',
  take = 'pull',
}
